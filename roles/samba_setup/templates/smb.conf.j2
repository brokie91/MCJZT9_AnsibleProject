# {{ ansible_managed }}
[global]
   workgroup = {{ samba_workgroup }}
   security = {{ samba_security }}

   [guestshare] 
   comment = Guest access share
   path = {{ samba_share_path }}
   read only = yes
   guest ok = yes
